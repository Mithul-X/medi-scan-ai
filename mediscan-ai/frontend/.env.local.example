# Copy to .env.local for local dev. On Vercel, set NEXT_PUBLIC_API_BASE_URL
# in Project Settings > Environment Variables instead (vercel.json's `env`
# block is a placeholder/default, not a substitute for the real dashboard value).

NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
